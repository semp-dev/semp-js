# semp-js

This directory is a redirect.

The JavaScript implementation of SEMP is provided by [`semp-ts`](https://github.com/semp-dev/semp-ts).

If you want to consume SEMP from JavaScript, use the published package
that `semp-ts` produces.

If you want to contribute, open issues and PRs against `semp-ts`.
